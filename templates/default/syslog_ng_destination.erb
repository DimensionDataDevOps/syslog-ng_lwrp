# Generated by Chef for <%= node['fqdn'] %>
# Local modifications will be overwritten.
#
# <%= node['syslog_ng']['config_dir'] %>/conf.d/<%= [@destination[:index], @destination[:name]].join %>
#

destination <%= @destination[:destination_prefix] %><%= @destination[:name] %> {
  <% @drivers.each do |driver| %>
    <%= driver['driver'] %>(
      <%= driver['options'] %>
    );
  <% end %>
};

# do not forget the empty line
